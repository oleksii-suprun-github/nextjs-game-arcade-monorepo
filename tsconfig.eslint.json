{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.base.json",
  "include": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", ".eslintrc.json"],
  "exclude": ["node_modules/**", "dist/**", "coverage/**"]
}
